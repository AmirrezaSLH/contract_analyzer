
0. create `.env` based on `.env.example`
1. run `bash setup.bash` to install dependencies
2. run `bash start.bash` to start the app
    -   Access the app through `localhost:8100`
3. run `bash stop.bash` to kill any orphaned process